services:
  - type: web
    name: smart-preprocessing-ai
    runtime: python
    pythonVersion: "3.13.0"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    rootDir: smart-preprocessing-ai